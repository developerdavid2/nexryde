import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import "../global.css";

const Home = () => {
  return (
    <SafeAreaView>
      <Text className="text-blue-800">Home</Text>
    </SafeAreaView>
  );
};
export default Home;
